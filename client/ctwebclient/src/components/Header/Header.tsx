import React from 'react';
import cl from './header.module.css';

const Header = () => {
    return (
        <div className={cl.header}>
            Header
        </div>
    );
};

export default Header;
