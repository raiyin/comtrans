import React from 'react';
import cl from './footer.module.css';

const Footer = () => {
    return (
        <div className={cl.footer}>
            Footer
        </div>
    );
};

export default Footer;
