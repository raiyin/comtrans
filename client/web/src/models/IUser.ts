export interface IUser {
    username: string;
    email: string;
    isActivated: boolean;
}

export interface UserDto {
    username: string;
    email: string;
    isActivated: boolean;
}
